# Progress Log

Recovery point for a resumed session. Updated every commit.
To resume: run `git log --oneline -20`, `git status`, then `cd backend && ../.venv/bin/pytest -q`
before trusting this file.

## Current phase

Phase 1 — Physics core.

## Modules done

- Project scaffold, `.gitignore`, `pyproject.toml`, `DECISIONS.md`, `PROGRESS.md`.
- `.venv` (Python 3.11.15) created.

## Tests passing

- (none yet)

## Next step

`core/units.py`, then `core/propellant.py`.

## Last files touched

- `.gitignore`, `backend/pyproject.toml`, `DECISIONS.md`, `PROGRESS.md`
